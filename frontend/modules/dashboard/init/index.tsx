import { Heading, Panel } from "@/modules/dashboard/init/components";

const Init = () => {
  return (
    <div className="space-y-24 py-10">
      <Heading />
      <Panel />
    </div>
  );
};

export default Init;

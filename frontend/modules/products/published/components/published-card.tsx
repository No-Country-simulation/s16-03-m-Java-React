
import Image from "next/image";
import Link from "next/link";

import { PublishedType } from "../constants/published-data";

type Props = {
data: PublishedType;
};

const PublishedCard = ({ data }: Props) => {
return (
    <>

    
    </>
);
};

export default PublishedCard;
